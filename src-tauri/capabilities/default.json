{
  "identifier": "default",
  "description": "Default capabilities for Mothership desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "shell:default",
    "shell:allow-open"
  ]
}
